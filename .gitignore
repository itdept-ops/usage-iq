# ---------- secrets / env ----------
.env
*.env
!.env.example
# Google OAuth client secret + JWT signing key + allowlist live here (never commit):
appsettings.Local.json
!appsettings.Local.example.json

# ---------- .NET ----------
[Bb]in/
[Oo]bj/
*.user
*.suo
.vs/
artifacts/
appsettings.*.local.json

# ---------- Node / Angular ----------
node_modules/
dist/
.angular/
npm-debug.log*
yarn-error.log*
*.tsbuildinfo

# ---------- IDE / OS ----------
.idea/
.vscode/*
!.vscode/extensions.json
.claude/
*.swp
Thumbs.db
.DS_Store

# ---------- misc ----------
*.log
coverage/
TestResults/

# Reporter ingest key saved by Run-UsageIqReporter.ps1 — secret, never commit
reporter.key
*.key

# Themeforest source template (design reference only — not part of the app)
ThemeforestMobileTemplate/
