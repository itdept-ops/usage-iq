<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Ccusage.Reporter.Core</RootNamespace>
    <AssemblyName>Ccusage.Reporter.Core</AssemblyName>
    <InvariantGlobalization>true</InvariantGlobalization>
    <!--
      This assembly is pulled into the distributed Windows Agent (src/Agent), which harvests WMI hardware
      inventory + GPS/geolocation and phones home. Pin every dependency to an exact version (no 9.0.*
      floats) and emit a committed packages.lock.json so the shipped binary restores from a bit-for-bit
      identical transitive closure on every dev machine / CI run (supply-chain reproducibility & attestation).
    -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Configuration" Version="9.0.17" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Binder" Version="9.0.17" />
    <PackageReference Include="Microsoft.Extensions.Configuration.CommandLine" Version="9.0.17" />
    <PackageReference Include="Microsoft.Extensions.Configuration.EnvironmentVariables" Version="9.0.17" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="9.0.17" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Ingestion\Ccusage.Ingestion.csproj" />
  </ItemGroup>

  <!--
    System.Management gives the WMI hardware probes (CPU/RAM/GPU/manufacturer/model/domain) in
    MachineInfo.Collect. It is a Windows-only API; all callers are guarded by OperatingSystem.IsWindows(),
    and the package itself no-ops at runtime off Windows. Referenced unconditionally so the assembly still
    compiles cross-platform (net9.0); Microsoft.Win32.Registry ships in the shared framework.
  -->
  <ItemGroup>
    <PackageReference Include="System.Management" Version="9.0.17" />
  </ItemGroup>

  <!-- Reuse the Usage IQ brand icon as a shared asset for the console + future desktop/tray app. -->
  <ItemGroup>
    <None Include="Assets\usage-iq.ico" CopyToOutputDirectory="PreserveNewest" Condition="Exists('Assets\usage-iq.ico')" />
  </ItemGroup>

</Project>
